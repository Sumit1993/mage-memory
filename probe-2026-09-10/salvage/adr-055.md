<!-- salvaged from subagent transcript agent-a9ccb7904f8425231.jsonl, spawned 2026-09-10T08:45:26.642Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0055-connect-doctor-migrate.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-mode__ctx_search(sort: "t

# Final report

## ADR-0055 — Connect, doctor, migrate

Clause table (D = Decision, F = Forbids). All file:line citations verified with `sed -n` against the worktree at commit a945759.

```
id  | clause (truncated)                                                          | (a) enforcer                        | (b) exists? file:line                                          | (c) outcome    | row
D1  | connect is the one setup act                                                | nothing can: design property        | weaker: cli-program.ts:487 `adopt` is a 2nd onboarding verb    | silent         | -
D2  | connect is opt-in                                                           | nothing can: absence-of-autorun     | package.json:29 scripts (no postinstall)                       | silent         | -
D3  | connect never bundled with the plugin install                               | nothing can w/o manifest-schema test| .claude-plugin/plugin.json (verified, no `hooks` key)          | silent         | -
D4  | writes marked, idempotent hook entries (id: mage:*) into settings.local.json| vitest test                          | settings.ts:63,80-129; settings.test.ts:172                    | fails-test     | -
D5  | refuses to touch malformed JSON                                             | vitest test                          | connect.ts:86-89; connect.test.ts:242                          | fails-test     | -
D6  | installs the Gate-2 hook                                                    | vitest test                          | connect.ts:447; git-hooks.ts:26; git-hooks.test.ts:36          | fails-test     | -
D7  | gitignores the capture sinks                                                | vitest test                          | connect.ts:434; gitignore.ts:9                                 | fails-test     | -
D8  | points native memory at the store                                           | vitest test                          | settings.ts:110,117 (mage:memory:Pre/PostToolUse)              | fails-test     | -
D9  | asks which kit accepts a pull request                                       | nothing enforces it: unbuilt         | none — no "kit?" prompt in connect.ts                          | silent         | -
D10 | asks which streams accept a pull request                                    | nothing enforces it: unbuilt         | none — no "stream" prompt in connect.ts                        | silent         | -
D11 | asks which landing scopes accept a pull request                             | nothing enforces it: unbuilt         | grooming/config.ts has no `landing` key                        | silent         | B12
D12 | writes the committed consent field and nothing else                        | nothing enforces it: unbuilt         | none found                                                     | silent         | B12
D13 | doctor audits capture, recall and readiness                                 | vitest test                          | doctor.ts:66-79 pushKbChecks; kb-checks.ts                     | fails-test     | -
D14 | doctor fails on a footprint breach                                          | vitest test + CLI exit code          | kb-checks.ts:891-917; cli-program.ts:546 process.exit(1); footprint.test.ts:97 | fails-test | -
D15 | --fix repairs only idempotent, mage-owned, local, reversible                | vitest test (narrow)                 | kb-checks.ts gitignore self-heal (doctor.ts:66)                | fails-test     | -
D16 | migrate is the only migration verb                                          | nothing enforces it                  | cli-program.ts:473,487 — `adopt` does parallel import          | silent         | A15
D17 | migrate clears retired state (0.0.x KB)                                     | vitest test                          | migrate.ts:83-223; migrate.test.ts (13 its)                    | fails-test     | -
D18 | migrate rewrites the AGENTS.md block w/o clobbering hand edits              | nothing enforces it: not implemented | none in migrate.ts (no agents-md.js import)                    | silent         | -
D19 | migrate repoints the memory hook                                            | nothing enforces it: not implemented | none in migrate.ts                                             | silent         | -
D20 | migrate installs the PreToolUse arm                                         | nothing enforces it in migrate.ts    | weaker: arm exists but connect installs it (settings.ts:110)   | silent         | A14
D21 | migrate warns on work/                                                      | nothing enforces it: not implemented | none in migrate.ts or migrate.test.ts                          | silent         | -
D22 | migrate imports existing notes through the ladder check                     | nothing enforces it in migrate.ts    | weaker: that logic is in `adopt` (cli-program.ts:487-502)      | silent         | A15
D23 | after 0.0.19 migrate folds into connect                                     | nothing can yet: no version gate     | package.json version "0.0.17" (below threshold)                | silent         | -
D24 | a retired verb prints its replacement and exits 0                          | nothing enforces it: unbuilt         | no retirement stub found in cli-program.ts/commands            | silent         | -
D25 | a retired verb never has a breaking marker                                  | nothing enforces it: unbuilt         | none — no verb has been retired this way yet                   | silent         | -
D26 | a person sees eight verbs (…groom, ledger, observe hidden)                  | vitest test — asserts the opposite   | generated-data.test.ts:43 (groom hidden=true); `ledger` absent from src/; 9+ extra visible verbs (skills,footprint,dream,link,unlink,verify,list,migrate,adopt,status,autonomy,dashboard) | silent | C5
F1  | Hooks installed by the plugin manifest                                      | nothing can w/o manifest-schema test | plugin.json (verified, no `hooks` key today)                   | silent         | -
F2  | Overwriting a hand-edited AGENTS.md block                                   | vitest test (partial)                | agents-md.ts:157-176 BEGIN/END slice; agents-md.test.ts:27     | fails-test     | A21
F3  | doctor --fix writing a machine path into a committed file                  | nothing enforces it directly         | adjacent gap: paths.ts:685 hub_path fallback into metadata.json| silent         | B2
F4  | A new verb for a migration                                                  | nothing enforces it                  | cli-program.ts — only "migrate" registered                     | silent         | -
F5  | A new visible verb outside the eight                                        | nothing enforces it: no total bound  | weaker: generated-data.test.ts:32-57 pins names, not count     | silent         | C5
F6  | `!` on a retirement                                                          | nothing enforces it                  | weaker: .github/workflows/pr-title.yml (generic Conv. Commits) | silent         | -
F7  | `BREAKING CHANGE` on a retirement                                           | nothing enforces it                  | weaker: .github/workflows/pr-title.yml (same generic check)    | silent         | -
F8  | A stamp scheme for the AGENTS.md block other than the content hash          | nothing enforces it: none exists     | agents-md.ts:13 — static BEGIN/END marker, no hash at all      | silent         | A21
```

Counts: 26 Decision clauses, 8 Forbids clauses, 34 total.
Outcomes: blocked 0, fails-test 10 (D4,D5,D6,D7,D8,D13,D14,D15,D17,F2), warns 0, silent 24 (D1,D2,D3,D9,D10,D11,D12,D16,D18,D19,D20,D21,D22,D23,D24,D25,D26,F1,F3,F4,F5,F6,F7,F8).

Doubt: D15 (fails-test vs silent — only the gitignore self-heal is tested, not the general idempotent/mage-owned/local/reversible property), F2 (fails-test vs silent — hand edits made inside the BEGIN/END block are unconditionally overwritten with no hash check), D26 (silent vs fails-test — a test exists but locks in the opposite of the ADR's claim).
