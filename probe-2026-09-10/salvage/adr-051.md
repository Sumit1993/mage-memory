<!-- salvaged from subagent transcript agent-a14ef5018fd3a819a.jsonl, spawned 2026-09-10T08:45:17.905Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0051-the-ladder-runs-before-anything-is-remembered.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-mo

# Final report

All citations confirmed by `sed`. Final output below.

---

ADR-0051 — The ladder runs before anything is remembered

Verified against worktree HEAD a945759. The admission/readability schema check referenced in ADR Relations (#231 PR #255, #253) lives only on unmerged branch `feat/group-c-admission-and-readability` (commit a755481), reporting-only, gated behind an off-by-default `--strict-admission` flag "wired nowhere" per that commit's own message. It is not part of a945759.

Clause table (id | clause | (a) enforcer | (b) exists? | (c) outcome | contradiction row)

D1 | Highest rung that can carry a fix wins | nothing can: judgment call on which rung "could" apply | no artifact tests it | silent | —
D2 | Lower rung only when every higher rung shown not to apply | admission check requiring `skipped` field | not on HEAD; a755481 (unmerged) is reporting-only | silent | —
D3 | Proposal records each skipped rung with its reason | same admission check | same as D2 | silent | —
D4 | Native memory stays on and pointed at the store | settings.ts commandeer gating + doctor "connection" check | settings.ts:107,231-292 (gating logic, confirmed); kb-checks.ts:505-515 (doctor check, advisory, not in CI) | warns | —
D5 | PreToolUse memory hook blocks the write | memoryPreToolUse | memory-hook.ts:93 exists but returns `rewrite` not `deny` for topic notes (line ~125-137) | silent | B6
D6 | ...returns the ladder | MemoryDecision type | memory-hook.ts:39-48 — kind is only deny/rewrite/pass, no ladder field | silent | —
D7 | Note names its skipped rungs | admission check (`skipped`) | a755481 only, unmerged, reporting-only | silent | —
D8 | ...its trigger moment | admission check (`trigger`) | a755481 only, unmerged, reporting-only | silent | —
D9 | ...and its pointer | admission check (`pointer`) | a755481 only, unmerged, reporting-only | silent | —
D10 | Same hook fires on a direct write under `notes/` | flatInboxRel scope check | memory-hook.ts:76 — subdir write returns null, "PASS untouched" (comment line 12) — opposite of the clause | silent | —
D11 | `mage index` rejects a note without those fields | index-cmd frontmatter validation | none on HEAD; confirmed no `checkAdmission`/reject logic in src/commands/index-cmd.ts | silent | —
D12 | If the hook fails, a `notes/` write is denied | try/catch boundary | memory-hook.ts:242-243 — fail-open, exits 0 on any error, opposite of the clause | silent | B6, A18
D13 | ...and every other write passes | same try/catch | memory-hook.ts:242-243 — fails open for everything, not selectively | silent | —
D14 | A note is insight, procedure, pointers, one screen, plain words | checkReadability (line/word/jargon check) | a755481 only, unmerged, reporting-only | silent | —
D15 | ...never a copy of a source | nothing can: content-judgment, no detector | none | silent | —
D16 | Expected to leave: climb a rung, or a delete proposal when trigger stops | a "delete" proposal kind in grooming | none found in proposals.ts/types.ts | silent | —
F1 | A note without trigger | admission check | a755481 only, unmerged | silent | —
F2 | A note without pointer | admission check | a755481 only, unmerged | silent | —
F3 | A note without skipped rungs | admission check | a755481 only, unmerged | silent | —
F4 | A copy of a source as a note | nothing can | none | silent | —
F5 | A hook that judges | nothing can: no test asserts the hook stays field-presence-only | none | silent | —
F6 | A ladder gate that fails open on `notes/` | try/catch boundary | memory-hook.ts:243 IS this forbidden state today | silent | B6, A18
F7 | A frontmatter type or genre deciding a recall rung (0041) | nothing named; would need a lint/check | contradicting code live: genre-map.ts:3 ("recall rungs...ADR-0041 §2"), skills-cmd.ts:260-261 ("per ADR-0041 §4") | silent | not in OUT-contradictions.md — new
F8 | Promoting a note to a skill by usage count | nothing named; would need a check on `graduate` | contradicting code live: promote.ts:7-8 ("READ in >= thresholds.graduateSessions distinct chapters...earns a Procedure skill"), types.ts:17 ("the graduation count") | silent | not in OUT-contradictions.md — new
F9 | A recurrence count as the ladder's test | same gap | same artifact as F8 (chapter-count IS the rung-promotion test for note→rule) | silent | not in OUT-contradictions.md — new

Counts: Decision clauses 16, Forbids clauses 9 (25 total).
Outcomes: blocked 0, fails-test 0, warns 1 (D4), silent 24.

Doubt: D4 — unsure between warns and silent (doctor's connection check exists but only fires on manual `mage doctor`, never in CI, so it warns sometimes and is silent the rest of the time; committed to warns). D13 — unsure between silent and "not violable" (the current blanket fail-open trivially satisfies it as a side effect of violating D12, not by design; committed to silent).
