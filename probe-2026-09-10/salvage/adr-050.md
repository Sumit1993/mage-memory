<!-- salvaged from subagent transcript agent-a0cf5c0a0069ca365.jsonl, spawned 2026-09-10T08:45:15.722Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0050-mage-turns-a-repeated-failure-into-enforcement.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-m

# Final report

ADR-0050 — mage turns a repeated failure into enforcement; memory is the queue

Clause table (D = Decision, F = Forbids). All file:line citations verified with `sed -n` against a945759.

| id | clause (short) | (a) enforcer | (b) exists? | (c) outcome | row |
|---|---|---|---|---|---|
| D1 | "mage is the loop that turns a repeated failure into a guard." | nothing can: identity statement | n/a | silent | |
| D2 | "Streams feed observe events" | PreToolUse/PostToolUse/Stop hooks writing `.learnings/*.jsonl` | settings.ts:80-101 hook table (verified) | silent (hooks fail-open, no check confirms a stream kept flowing) | B5 |
| D3 | "code narrows on first sight" | deterministic distill (no LLM) dedup logic | src/distill/reader.ts (segment/salience extraction, no model call — verified no LLM import in src/distill,grooming,nudge) | silent | |
| D4 | "counts rank the digest" | sort-then-slice ranking | src/grooming/promote.ts:117-120 `eligible.sort(rankProposals)` then `.slice(0, budget)` | silent | |
| D5 | "[counts] never gate it" | same code path must NOT filter, only rank | **violated**: src/grooming/promote.ts:99-102 `if (stat.chapters < thresholds.graduateSessions) { climbing += 1; continue; }` drops the note from `eligible` before ranking — a recurrence count gates what the agent sees | silent (tests assert this gate as correct behavior, so CI is green on the forbidden pattern) | B4 |
| D6 | "at most three proposals per pass" | a `promotionBudget`/proposal-cap constant of 3 | weaker/mismatched: `thresholds.ts:65 promotionBudget: 5`; a different budget `thresholds.ts:71 stagingBudget: 3` caps staged drafts, not "proposals"; ADR's `proposal` type doesn't exist in src/ | silent | |
| D7 | "the agent judges the highest rung the fix can reach" | a hook/CLI output that hands the agent a rung choice, no auto-pick | nothing enforces it — no `rung` concept matching {impossible,check,hook,rule,note} exists; `rung` in code (autonomy-ladder.ts, dashboard/html.ts:617) is the old scratch/notes/skills ladder, a naming collision not this mechanic | silent | |
| D8 | "the fix lands by pull request" | `gh pr create`/push code path | nothing enforces it — no branch/push/PR code in `src/` (verified: no match) | silent | B11 |
| D9 | "a ledger counts how often it fires" | a `ledger` module/type | nothing enforces it — no ledger concept in src/ (grep found none) | silent | B4/B12 |
| D10 | "what never fires is proposed for deletion" | a stale-guard scan | nothing enforces it | silent | |
| D11 | "mage has no model" | dependency allowlist / lint rule banning LLM SDKs | nothing enforces it — package.json:62-69 deps have no LLM SDK today, but no check blocks adding one | silent | |
| D12 | "mage never judges" | same as D11, plus "no reasoning in CLI" check | nothing enforces it | silent | |
| D13 | "a hook makes the agent judge" | hook contract (nudge/observe output is data, not a verdict) | weaker: nudge.ts two channels never block (settings.ts:87, inventory item 4), consistent but not asserted by any test naming this invariant | silent | |
| D14 | "Five words and no others." | a vocabulary/glossary lint over decision docs | nothing enforces it | silent | |
| D15 | "A guard is any landed artifact, at any rung." | definition | n/a | silent | |
| D16 | "A rung is impossible, check, hook, rule or note." | definition | n/a | silent | |
| D17 | "A fire is one counted event attributed to a guard id." | definition | n/a | silent | |
| D18 | "A proposal is a guard not yet landed." | definition | n/a | silent | |
| D19 | "A unit is one knowledge base with its own ledger." | definition | n/a | silent | A16 |
| D20 | "Plans and evidence live in the issue tracker" | AGENTS.md sentence + doctor/pre-commit check rejecting `mage/work/*.md` | weaker: AGENTS.md:28-30 sentence only (per inventory item 11); no check blocks a `mage/work/*.md` commit | **violated live**: `git ls-files mage/work` lists 15 tracked plan files incl. roadmap.md, plan-v0.1.md (verified) | A2/B1 |
| D21 | "decisions and notes live in the knowledge base" | KB structure doctor check | doctor/kb-checks.ts:233 KB structure check | blocked-ish/warns (doctor flags structure drift, non-blocking) | |
| D22 | "a decision fits on one screen" | a line-count check on `mage/decisions/*.md` | nothing enforces it — 0050 itself is 35 lines by eye, no CI check measures it | silent | A5 |
| F1 | "A threshold or recurrence count that gates what the agent sees." | same code path as D5 | **violated**, same citation as D5 (`promote.ts:99-102`, `graduateSessions` in thresholds.ts:59/93-97) | silent | B4 |
| F2 | "A reasoner in the CLI" | dependency allowlist | not currently present (no LLM SDK dep, verified) but no automated check prevents adding one | silent | |
| F3 | "a hook that reasons" | same | not currently present (verified no LLM import in hook code) | silent | |
| F4 | "Coordination primitives between agents." | dependency/import check for queue/pubsub libs | none found; not enforced | silent | |
| F5 | "Depending on another memory product's engine." | dependency allowlist | package.json:62-69 has no vector-DB/mem0/zep-style dep today; no check bans one | silent | |
| F6 | "Spec-driven-development skills." | plugin skill inventory check | none found under `skills/` (item 10: graduate, groom, guide, learn, optimize only); no automated check | silent | |
| F7 | "A plan or task list committed as a repo file." | pre-commit or CI path check rejecting `mage/work/**` | nothing enforces it | **violated live**, same citation as D20 | A2/B1 |
| F8 | banned words (lesson, learning, chapter, wing, room, staged, admitted, graduate) outside observe schema | grep-based doctor/CI check | **violated live**: `src/agents-md.ts:85,87,124,126` template text itself contains "Capture lessons", "stage", "wing/room", ".staging/"; `doctor/kb-checks.ts:34,81,222-227,298,705,732-733,811,845` use `learnings`/`staged` as identifiers throughout | silent (no check scans for these tokens; `doctor.ts:336` AGENTS.md-awareness check only flags *retired command names*, not this word list — confirmed by reading kb-checks.ts:320-345) | A17 |

Counts: Decision clauses = 22 (D1–D22). Forbids clauses = 8 (F1–F8). Total = 30.
Outcomes: blocked = 0, fails-test = 0, warns = 1 (D21), silent = 29.

Doubt: D13 (silent vs warns — nudge's fail-open design arguably counts as an implicit "warns" channel even though nothing asserts the invariant); D21 (warns vs silent — doctor's KB-structure check is opt-in/non-blocking, close to silent in practice).
