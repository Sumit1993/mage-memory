<!-- salvaged from subagent transcript agent-a824f93835164f495.jsonl, spawned 2026-09-10T08:45:30.712Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0057-a-guard-lands-by-pull-request.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-mode__ctx_search(s

# Final report

# ADR-0057 — A guard lands by pull request; the human merges

Worktree `.claude/worktrees/fable-notes-probe` at `a945759`. 28 Decision clauses, 9 Forbids clauses (F6 split into two sources).

| id | clause (short) | (a) enforcer | (b) exists? | (c) outcome | contradiction |
|---|---|---|---|---|---|
| D1 | "only way a guard lands is a branch and a pull request" | GitHub branch ruleset blocking direct pushes to main | weaker: `.github/governance.json:91` `pull_request` rule + `:89` `non_fast_forward` (generic, not guard-specific; no guard-authoring/PR code exists — no `gh pr create` in `src/`) | blocked | — |
| D2 | "opened when explicitly invoked (`groom --propose`)" | a `--propose` flag on `groom` gating PR-open | nothing enforces it — `groom` has no `--propose` (cli-program.ts:295-321) | silent | — |
| D3 | "never as a side effect of a hook" | check that no hook path opens a PR | nothing enforces it — no PR-opening code exists anywhere | silent | — |
| D4 | "Landing scopes: the code repo and the kit... at connect" | scope allowlist in the applier | nothing enforces it — no applier | silent | — |
| D5 | "an org workflows repo comes later" | nothing can: statement of deferred intent | n/a | silent | — |
| D6 | "Consent is a committed field (the `landing` key...)" | `landing` key read in grooming config | nothing enforces it — `GroomingConfig` has only sensitivity/autonomy/nudgeThrottleHours/crownThreshold (`src/grooming/config.ts:31-40`), no `landing` | silent | B12 |
| D7 | "never a machine-local or environment carrier" | consent-reader rejecting env/local sources | nothing enforces it — no consent reader exists at all | silent | — |
| D8 | "a scope is writable when consent is present and its repo is reachable" | consent+reachability gate | weaker: generic reachability probe exists (`src/commands/doctor.ts:192-211`) but not wired to any writability decision | silent | — |
| D9 | "Types: deny, hook, check, rule, skill, note, delete" | type enum validated by the applier | nothing enforces it — no guard-type vocabulary in `src/` | silent | — |
| D10 | "rung must match type" | applier validation | nothing enforces it | silent | — |
| D11 | "a hook carries a test" | applier validation | nothing enforces it | silent | — |
| D12 | "a check lands as an issue" | applier action | nothing enforces it | silent | — |
| D13 | "`landed` only when the guard inventory finds a CI job named with its id" | a guard-inventory checker | nothing enforces it — no `GuardInventory`/landed-detection code (grep confirmed) | silent | A19 |
| D14 | "One applier is the single writer" | single-entrypoint architecture for guard writes | weaker: the pattern exists for a different domain — `src/index.ts:304-306` names the dream applier "the one" writer for notes/skills, not guards | silent | — |
| D15 | "it mints `<scope>/<kind>/<slug>`" | id-minting logic | nothing enforces it | silent | — |
| D16 | "stamps provenance" | provenance stamp call inside the guard applier | weaker: `stampProvenance()` exists and works (`src/provenance.ts:57`) but nothing guard-shaped calls it | silent | — |
| D17 | "refuses a missing skipped reason" | applier refusal | nothing enforces it | silent | — |
| D18 | "refuses an illegal type-rung pair" | applier refusal | nothing enforces it | silent | — |
| D19 | "refuses a hook without a test" | applier refusal | nothing enforces it | silent | — |
| D20 | "refuses a note without trigger and pointer" | applier refusal | weaker: AGENTS.md sentence asks for "insight + procedure + pointers" as authoring guidance, not a machine check | silent | — |
| D21 | "refuses a delete without a target" | applier refusal | nothing enforces it | silent | — |
| D22 | "A guard is one committed file per id, `guards/<id>.md` beside `notes/`" | fixed path convention scanned like `notes/` | nothing enforces it — no `guards/` handling anywhere in `src/` (grep hits only the ADR itself) | silent | A20 |
| D23 | "the six carriers are outputs of landing" | nothing can: taxonomic statement over D9/D12 | n/a | silent | — |
| D24 | "the compiled table under `.mage/` is a cache" | `.mage/` excluded from git and from scan | `.gitignore:18` `mage/.mage/`; `src/scan.ts:38-45` `SKIP_DIRS` includes `STATE_DIR` | blocked | — |
| D25 | "Autonomy... sets how much the agent drains alone" | autonomy ladder gating unsupervised writes | weaker: built for note-grooming, not guards — `src/grooming/autonomy-ladder.ts:14-90` (mandate text, not a mechanical block) | warns | — |
| D26 | "the merge is the human gate at every level" | branch ruleset forcing a human merge click | weaker: `governance.json:93` `required_approving_review_count: 0`; `:9` `allow_auto_merge: true` — a PR can merge with no human approval once checks pass | warns | — |
| D27 | "A skill is one output rung, measured by firing" | context-match firing metric | `src/commands/skills-cmd.ts:145` read-only context-match; `src/grooming/promote.ts:24-25` "reword/demote come from `mage skills --metrics`" — advisory, human confirms via `mage dream --apply` | warns | — |
| D28 | "never a note promoted by usage count" | absence of a recurrence-count promotion path | contradicts in spirit: `src/grooming/promote.ts:18-21` graduates a note to a skill by chapters-read count, i.e. a usage count | warns | — |
| F1 | "Committing to a default branch." | branch ruleset | `governance.json:88` `deletion`, `:89` `non_fast_forward`, `:91` `pull_request` | blocked | — |
| F2 | "Pushing outside the proposal branch." | branch-scope check limiting guard pushes | nothing enforces it — ruleset targets only `DEFAULT_BRANCH`; no applier/branch-naming code | silent | — |
| F3 | "Deleting on disk (a delete is a PR diff)." | delete-type must route through PR, never direct `rm` | nothing enforces it — no delete-guard type; no hook restricts direct file deletion | silent | — |
| F4 | "A proposal without skipped reasons." | applier refusal | nothing enforces it (= D17) | silent | — |
| F5 | "Marking a check landed on issue closure alone." | guard inventory, not issue state | nothing enforces it — no guard inventory exists | silent | A19 |
| F6a | "Consent from an environment variable" | consent-reader restricted to committed sources | nothing enforces it — no consent reader exists | silent | — |
| F6b | "[Consent from] a local file" | same | nothing enforces it | silent | — |
| F7 | "A threshold in the proposer." | no numeric threshold in the guard proposer | nothing enforces it — no guard proposer exists; weaker/adjacent: `crownThreshold` is a real threshold in the sibling note-graduation proposer (`src/grooming/config.ts:38-39`) | silent | — |
| F8 | "Guard files under transient `.mage/`." | rejection of guard writes under `.mage/` | nothing rejects it; incidentally `.mage/` is gitignored and unscanned (same citations as D24), so such a file would be invisible, not refused | silent | — |

## Counts
- Decision clauses: 28. Forbids clauses: 9 (F6 split into F6a/F6b).
- blocked: 3 (D1, D24, F1)
- fails-test: 0
- warns: 4 (D25, D26, D27, D28)
- silent: 30 (all remaining)

## Doubt
Unsure between two outcomes on: D1 (blocked vs silent — the block is a generic branch ruleset, not guard-specific machinery), D26 (warns vs blocked — a PR is forced, but `required_approving_review_count: 0` and `allow_auto_merge: true` let it merge with no human click), D28 (warns vs silent — today's note-to-skill graduation demonstrably promotes by a usage/read count, which reads as a live violation rather than mere absence), D16 (silent vs warns — the provenance primitive is real and tested but no guard code path reaches it).
