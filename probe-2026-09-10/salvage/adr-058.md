<!-- salvaged from subagent transcript agent-a8d523a77989ecd0d.jsonl, spawned 2026-09-10T08:45:32.738Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0058-recall-one-bounded-index.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-mode__ctx_search(sort: 

# Final report

## ADR-0058 — Recall is one bounded index, loaded every session

Worktree: `/home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe` at a945759. All file:line citations below were re-verified with `sed -n '<line>p'` against that checkout; none were taken from `_inventory.txt` without independent confirmation.

| id | clause (short) | (a) enforcer | (b) exists? file:line | (c) outcome | row |
|---|---|---|---|---|---|
| D1 | `mage index` generates `INDEX.md` (one line per note: type, title, keywords, link) | vitest test on rendered line format | yes: `src/commands/index-cmd.ts:645-650` (closetLine), tested `index-cmd.test.ts:650-710` | fails-test | — |
| D2 | `MEMORY.md`, the roster Claude Code auto-loads | none — external harness behavior | n/a | silent | — |
| D3 | AGENTS.md block carries a managed `@mage/INDEX.md` import line as the fallback | none for the literal `@`-syntax | weaker: prose link only, `AGENTS.md:13` ("Read `mage/INDEX.md` first"); `agents-md.ts:14` `CLAUDE_IMPORT = "@AGENTS.md"` wires CLAUDE.md→AGENTS.md, not AGENTS.md→INDEX.md — no `@mage/INDEX.md` string anywhere in `src/` | silent | A9 |
| D4 | roster lists only notes that passed the ladder | none — no admission-ladder check in the generator | weaker: `index-cmd.ts:68-71` filters by genre==memory only, no rung/promotion gate; tracked by #245 (unbuilt) | silent | — |
| D5 | roster is in the order they passed | none — code sorts by tally-proven flag then recency, not passage order | contradicts: `index-cmd.ts:338-365` `rankMemoryNotes` (proven-first, then recency, then relPath) | silent | — |
| D6 | roster bounded by the harness cap (25,600 bytes, 200 lines) | constant + doctor breach check | yes: `src/adapters/claude-code/constants.ts:7-8` (`AUTO_MEMORY_MAX_BYTES=25_600`, `AUTO_MEMORY_MAX_LINES=200`); breach path `src/doctor/kb-checks.ts:912-921` | warns | — |
| D7 | `footprint` warns at 70 percent | constant + state calc | yes: `src/metrics/footprint.ts:16` `WARN_RATIO = 0.7`, applied `:169` | warns | — |
| D8 | `doctor` fails at 90 | constant + doctor check | yes: `footprint.ts:17` `BREACH_RATIO = 0.9`, `kb-checks.ts:912` `if (state === "breach")`, `ok:false, optional:false`; `doctor.ts:111` `passed = checks.every(...)`, `cli-program.ts:459` `process.exit(1)` | warns | — |
| D9 | nothing is silently truncated | overflow line always emitted + warn log | yes: `index-cmd.ts:482` `overflowLine(...)` unconditionally pushed into content; `index-cmd.ts:545-561` `logger.warn` on cut/shed | warns | — |
| D10 | per-project skills are generated | generator | yes: `src/commands/skills-cmd.ts:77` (one auto-loaded skill per wing) | warns | — |
| D11 | skills auto-load | none — host plugin loading, mage only checks reachability | weaker: `doctor.ts:171` `checkSkillsInstall()`, comment at call site: "Advisory, never a hard fail" | silent | — |
| D12 | skill firing counted by context-match over a 20-event window | vitest test | yes: `src/metrics/context-match.ts:16` `MATCH_WINDOW = 20`, used `:211`; `context-match.test.ts` (17 tests) | fails-test | — |
| D13 | decisions and plans are never in the roster | genre filter | yes: `index-cmd.ts:68-71` `memoryEntries` filtered to `genreOf(e.type) === "memory"` | fails-test | — |
| D14 | recall at moment of need comes after 0.1.0 (#201) | none — roadmap deferral, no artifact yet | n/a (nothing built) | silent | — |
| F1 | forbids importing note bodies | fixed render template, no body field read | yes: `index-cmd.ts:645-650` closetLine never touches `.body`; zero references to `.body` in `index-cmd.ts`/its test | silent | — |
| F2 | forbids importing `_index.mage.md` | none explicit; current code only links, doesn't import | weaker: `src/agents-md.ts` external-hub block builds `projIndex` as a markdown link, not an `@`-import | silent | — |
| F3 | forbids a tokenizer dependency | none | none found: no `tiktoken`/`gpt-tokenizer`/tokenizer entries in `package.json` or `src/` | silent | — |
| F4 | forbids a second frontmatter field deciding recall | none — schema is open (`[key: string]: unknown`, `src/note.ts:32-53`) and `rankMemoryNotes` reads no frontmatter field, only `tally`/dates | none | silent | — |
| F5 | forbids editing a generated file by hand | PreToolUse hook deny | yes: `src/adapters/claude-code/memory-hook.ts:112` `if (isGeneratedArtifact(rel)) return { kind: "deny", ... }` | blocked | — |
| F6 | forbids ranking the roster by a recurrence tally | none — code does the opposite | contradicts: `index-cmd.ts:338-352` `rankMemoryNotes` step 1 sorts by `noteChapters(relPath) >= graduateSessions` (the promote tally), citing ADR-0041 Amendment §3 in its own comment (`:333-336`, `:51-56`) | silent | — |

Counts: Decision clauses 14, Forbids clauses 6, total 20.
Outcomes: blocked 1, fails-test 3, warns 5, silent 11.

Doubt: D6/D8 — warns vs fails-test (`mage doctor` exits 1 on breach, but no CI job or hook consumes that exit code per `_inventory.txt` §2, so it never actually gates anything). D13 — fails-test vs silent (the genre filter is exercised by existing roster-render tests, but none of them assert specifically that a `decision`- or `plan`-genre entry gets excluded).
