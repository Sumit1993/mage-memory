<!-- salvaged from subagent transcript agent-a7a7be2c946092c72.jsonl, spawned 2026-09-10T08:45:18.696Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0052-streams-and-the-observe-schema.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_context-mode__ctx_search(

# Final report

ADR-0052 — Streams and the observe schema

Clause table (D=Decision, F=Forbids):

| id | clause | (a) enforcer | (b) exists? | (c) outcome | contradiction |
|---|---|---|---|---|---|
| D1 | "A stream is anything that emits observe events." | definition; type union is the closest artifact | src/observe/types.ts:9-16 `ObserveEventType` union | silent — no test checks a stream matches the definition | — |
| D2 | "The envelope is v1 and additive only" | vitest test on reader tolerance of unknown fields/types | nothing enforces it; only a comment, types.ts:2-4 | silent | — |
| D3 | "new event types are appended" | same as D2 | weaker: comment only, types.ts:2-4 | silent | A14 |
| D4 | "existing ones are never reopened" | same as D2 | weaker: comment only, types.ts:2-4 | silent | A14 |
| D5 | "there is no `seq`" | type shape (no `seq` field) + single-writer append order | src/observe/store.ts:88 (comment) confirms causal-order-by-line-order design; no `seq` field in types.ts | silent (nothing stops a future field add) | — |
| D6 | Built-in streams: Claude Code hooks (8 named) | settings.ts hook-registration array | src/adapters/claude-code/settings.ts:80 (SessionStart) wires 7 of 8; PreToolUse is NOT wired to `mage observe` (only commandeer-gated `mage memory-hook`, settings.ts:109-114) | silent (partial; lag tracked, not caught by any check) | B5 |
| D7 | Built-in streams: operator corrections | derived-signal code (adjacency of user_prompt after tool_use) | weaker: src/distill/reader.ts:111 `if (prevType === "tool_use") corrections.push(e.text)` — no distinct event type, no rule id | warns (surfaces via digest, nothing blocks) | — |
| D8 | Built-in streams: kit hooks reporting `guard_fired` | event-type union entry + hook wiring | nothing enforces it; `guard_fired` absent from types.ts:9-16, no grep hits in src | silent, not built | C12 |
| D9 | Built-in streams: one review-findings puller writing `finding {...}` | a puller module/CLI command | nothing enforces it; no puller code in src (grep for review-findings/coderabbit/pull review: zero hits) | silent, not built | C10 |
| D10 | "`tool_attempt` and `tool_use` both carry the harness invocation id" | event field in the type + mapping code | nothing enforces it; no `tool_attempt` type, no invocation-id field anywhere in types.ts | silent, not built | B5 |
| D11 | "an attempt with no matching use is a prevented call" | counting logic reading `tool_attempt`/`tool_use` pairs | nothing enforces it; premise (D10) doesn't exist | silent, not built | C6 |
| D12 | "Every stream writes through `mage observe` on stdin so Gate-1 scrubs it" | hook wiring + scrubField call inside the command | src/commands/observe.ts:22,211,232,309,316,328 call `scrubField`; settings.ts routes 7/8 hooks to `mage observe` | fails-test for the wired paths (observe.test.ts asserts scrub, e.g. line 396); silent for any new stream that could bypass the pipe | — |
| D13 | "nothing appends to `.mage/learnings/` directly" | import-boundary convention, no access control | weaker: only src/commands/observe.ts imports observe/store.ts (grep confirms sole importer); no permissions.deny, no lint rule | silent | — |
| D14 | "Streams carry no weights: a finding is real ... or it does not." | absence of a weight field + no test asserting absence | nothing enforces it; no `weight` field in src (grep clean), no test guards it | silent | — |
| D15 | "Narrowing is deterministic" | vitest test on distill/digest determinism | weaker: digest.test.ts covers dedup/ordering behavior but no test titled or scoped to "determinism" of narrowing itself | warns (behavior is deterministic by code structure, not asserted) | — |
| D16 | "the digest is chronological" | vitest test on sort order | src/distill/digest.ts:339 `sort((a,b) => a.firstIdx - b.firstIdx)`; digest.test.ts:72 "...stays chronological" | fails-test | — |
| F1 | "An event written to the log by anything but `mage observe`." | same as D13 | weaker: sole-importer convention only | silent | — |
| F2 | "A schema change a v1 reader cannot skip." | same as D2 | weaker: comment only, types.ts:2-4 | silent | A14 |
| F3 | "A producer-specific weight." | same as D14 | nothing enforces it | silent | — |
| F4 | "A digest sorted by count." | same as D16 | digest.ts:339 + digest.test.ts:72 | fails-test | — |
| F5 | "Storing a copy of tool output beyond the scrubbed detail." | type shape (no raw-output field) + scrub-only extraction path | src/observe/types.ts:109 comment "a salient extract, not a transcript copy"; ToolUseEvent interface (types.ts:110+) has no raw field; observe.ts:309 `mapToolUse` only stores `extractDetail(...)` through `scrubField`; observe.test.ts:396 asserts `JSON.stringify(e)).not.toContain(SECRET)` | fails-test | — |

Counts:
- Decision clauses: 16 (D1-D16)
- Forbids clauses: 5 (F1-F5)
- Total: 21
- blocked: 0
- fails-test: 4 (D12, D16, F4, F5)
- warns: 2 (D7, D15)
- silent: 15 (D1, D2, D3, D4, D5, D6, D8, D9, D10, D11, D13, D14, F1, F2, F3)

Unsure between two outcomes: D12 (fails-test for the wired hook paths vs. silent for the bypass case — committed to fails-test since that's what a test currently exercises); D15 (warns vs. silent — committed to warns since digest.test.ts exercises adjacent dedup/ordering behavior even without a determinism-titled assertion); D7 (warns vs. silent — committed to warns since the correction signal does surface through the nudge digest even though nothing enforces the "rule id" framing).
