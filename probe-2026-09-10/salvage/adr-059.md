<!-- salvaged from subagent transcript agent-a899a788dea646de9.jsonl, spawned 2026-09-10T08:45:34.986Z; the parent planner was killed by the session limit at 08:52 -->

# Task

Follow the brief at /home/sumit/ai-context/notes-probe/_adr-worker-brief.md exactly. Your decision file: /home/sumit/sources/mage-memory/.claude/worktrees/fable-notes-probe/mage/decisions/0059-versions-are-mechanical-the-release-is-two-counts.md
<context_window_protection>
  <priority_instructions>
    Every byte a tool returns enters your conversation memory and costs reasoning capacity for the rest of the session. The context-mode tools let you do the work in a sandbox and surface only the derived answer — the raw bytes stay out. Think-in-Code: program the analysis, do not compute it by reading raw data into your conversation.
  </priority_instructions>

  <deferred_tool_bootstrap>
    The context-mode tools below may be DEFERRED in your harness — their schemas are not loaded yet, so calling them directly fails (e.g. "tool not found" / InputValidationError). Load them ONCE before your first ctx_* call:
    ToolSearch(query: "select:mcp__plugin_context-mode_context-mode__ctx_batch_execute,mcp__plugin_context-mode_context-mode__ctx_search,mcp__plugin_context-mode_context-mode__ctx_execute,mcp__plugin_context-mode_context-mode__ctx_execute_file,mcp__plugin_context-mode_context-mode__ctx_fetch_and_index")
    After that they are callable. If any ctx_* call fails as not-found, ToolSearch it and retry — do NOT fall back to Bash/Read just because the schema was not loaded yet.
  </deferred_tool_bootstrap>

  <tool_selection_hierarchy>
    0. MEMORY: mcp__plugin_context-mode_contex

# Final report

# ADR 0059 — Versions are mechanical; the release is named by two counts

| id | clause | (a) enforcer | (b) exists? | (c) outcome | contradiction |
|----|--------|--------------|-------------|-------------|---------------|
| D1 | release-please mints versions from conventional commits | GH Action + required PR-title check | release-please.yml:26 (googleapis/release-please-action@v5); governance.json:107-110 required_status_checks incl. "Validate PR title" | blocked | |
| D2 | bump-minor-pre-major on, `!`/BREAKING CHANGE mints 0.1.0 | release-please config field | release-please-config.json:3 `bump-minor-pre-major: true` | silent (hand-edit bypass uncaught) | |
| D3 | No version is reserved | nothing can: no milestone-version binding exists | nothing enforces it | silent | |
| D4 | none is a milestone | same as D3 | nothing enforces it | silent | |
| D5 | announcement is a named GH release whose notes quote two counts from the ledger | none — would need a notes-template step | nothing enforces it (release-please auto-fills notes from commits only) | silent | |
| D6 | `prevented`: a guard fired (denied call, hook block/rewrite, failed check) | a counting mechanism over observe/guard events | nothing built in this repo; stopgap lives in claude-kit's transcript miner | silent | B5, C6 |
| D7 | prevented above zero, three distinct guards, rungs 1-3 | a release gate reading the ledger | nothing enforces it | silent | C8 |
| D8 | at least one guard on a unit other than mage-memory | same gate, needs "unit" concept | nothing enforces it; "unit" has competing definitions | silent | A16 |
| D9 | `left the queue`: a note climbed or deleted | ledger writer distinct from keep-rate reconcile.ts | weaker: reconcile.ts:15,99-245 tracks keep/edited/discard/reject, not "climbed to a guard" | silent | |
| D10 | left the queue above zero | same gate as D9 | nothing enforces it | silent | |
| D11 | ledger derived from observe events under `.mage/`, per unit | an aggregation script over observe logs | nothing built; observe/types.ts:10-17 has no guard/prevented event type | silent | B5 |
| D12 | an event counts in the unit whose observe log received it | same aggregation | nothing built; no per-unit attribution code | silent | A16 |
| D13 | nothing new is committed (the ledger) | gitignore exclusion of state dir | .gitignore:18 `mage/.mage/` (weaker: default-only, `add -f` bypasses) | blocked | A4 |
| D14 | Docs are a static site generated from code with a CI drift test | vitest drift assertion in required CI test | generated-data.test.ts:15-19; ci.yml:56 `pnpm test`; governance.json:108 "CI gate" required | fails-test | |
| D15 | no page states a number, hook or verb it did not derive from the code | same drift test, scoped assertions | generated-data.test.ts:22-57 covers only Thresholds/Hooks/Commands tables; other docs pages unchecked | fails-test | |
| D16 | Dogfood on a real knowledge base before a release | none — no pre-release check | weaker: mage/notes/dogfood-before-release.md is a reminder only | silent | |
| F1 | Forbids: a breaking marker on a deprecation | semantic PR review (no automated check) | pr-title.yml only validates Conventional Commits syntax, not semantics | silent | |
| F2 | Forbids: an evidence ADR per release | none — nothing blocks writing an ADR | nothing enforces it | silent | |
| F3 | Forbids: a version reserved for a milestone | none — no milestone-version binding in governance.json | nothing enforces it | silent | C1 |
| F4 | Forbids: a hand-typed load-bearing number in the docs | drift test, scoped to 3 generated tables | generated-data.test.ts covers Thresholds/Hooks/Commands only, not other docs prose | silent | |
| F5 | Forbids: a committed ledger | gitignore exclusion of state dir | .gitignore:18 `mage/.mage/` (weaker: default-only) | blocked | A4 |
| F6 | Forbids: emojis in release notes | none — no lint scans release notes/CHANGELOG | weaker: mage/notes/no-emojis-in-releases.md is a reminder only | silent | |

Counts: Decision clauses 16, Forbids clauses 6, total 22.
Outcomes: blocked 3 (D1, D13, F5) · fails-test 2 (D14, D15) · warns 0 · silent 17.

Doubt: D2 (blocked vs silent — automatic today, but a hand-edited version bypassing the rule is uncaught), D13 and F5 (blocked vs warns — gitignore is a default-only exclusion, not a hard deny), D15 (fails-test vs silent — enforced for the three generated tables, unenforced for the rest of the docs site).
