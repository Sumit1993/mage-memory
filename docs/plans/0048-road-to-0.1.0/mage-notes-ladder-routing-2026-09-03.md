## Summary table

| rung | count |
|---|---|
| impossible | 1 |
| check | 11 |
| hook | 5 |
| rule | 9 |
| note | 8 |
| delete | 5 |

## Note routing table

| file | rung | mechanism (concrete: the check/hook/rule/deny you would write, one line) | where it lands (repo path, claude-kit, or harness settings scope) | trigger moment (only for rung=note, else blank) | why |
|---|---|---|---|---|---|
| ad-hoc-measurement-scripts-are-code.md | rule | AGENTS.md rule: "cross-check every number before it enters a decision with an independent method; print the derivation, not just the total" | mage-memory AGENTS.md | | No program can detect whether a measurement was cross-checked; the rule applies at ADR-drafting time where a human is always present. |
| agent-maintained-files-carry-standing-rules-only.md | rule | AGENTS.md rule: "when editing CLAUDE.md or a skill, state rules in present tense; delete origin stories and one-off negative statements" | global CLAUDE.md or claude-kit skill | | The failure is a writing-style choice at edit time; no linter can distinguish a rule from its backstory. |
| agentmemory-mining-map.md | delete | Already compressed; full content in git history; decisions it drove (ADR-0007/0009/0010) are ratified and implemented. | n/a | | The note itself says "re-verify against their CHANGELOG before acting on any verdict" -- the operational conclusions are owned by the ADRs it references, not this survey. |
| agy-commit-message-compliance-is-unreliable.md | hook | commit-msg hook that runs `git log -1 --format=%B` and diffs against the byte-exact expected message stored in a prompt-supplied reference | mage-memory .git/hooks/commit-msg (harness settings) | | Calibration example: a commit-msg hook catches the exact failure at the moment the commit is made. |
| boundary-nudge-internals.md | note | (no mechanism; this is implementation documentation) | n/a | When editing nudge.ts, nudge-state.ts, or digest.ts, or when the nudge behaves unexpectedly at session start | The note describes internal wiring of a live component; the trigger is exactly "agent opens these files to modify them or diagnose unexpected nudge behavior." |
| connect-doesnt-ensure-ignores.md | check | Unit test: after calling `mage connect`, assert `git check-ignore -q .learnings/x .metrics/x` exits 0 for the connected KB | mage-memory src/commands/connect.test.ts (CI) | | The gap is in connect's code; a test that calls connect and then asserts ensureGitignored ran is the concrete check, and the note documents the fix location. |
| context-mode-blocks-files-outside-project-root.md | hook | PreToolUse hook intercepts ctx_execute_file calls where the path resolves outside the project root and rewrites to ctx_execute with inline fs.readFileSync | harness settings PreToolUse rewrite | | Calibration example: a PreToolUse rewrite removes the failure at the moment the blocked tool is called. |
| context.md | delete | Glossary and reference document; contains no single lesson to enforce -- it is the living terminology reference. | n/a | | No single enforcement target: the file is authoritative reference prose, not a behavioural lesson with a trigger moment. |
| delegation-prompts-must-name-real-commands.md | rule | AGENTS.md rule: "before delegating, verify every command you name exists: node -e \"console.log(Object.keys(require('./package.json').scripts))\"; distil repo house rules into the prompt" | global CLAUDE.md or mage-memory AGENTS.md | | No program can verify that a delegation prompt names only real commands before the prompt is written; the rule must be followed by the orchestrator at prompt-composition time. |
| dogfood-before-release.md | check | CI gate: pnpm test && pnpm run typecheck && pnpm build must be green before any tag; smoke-test procedure documented in plan-release-sequence.md | mage-memory .github/workflows/ci.yml | | The pnpm test/typecheck/build portion is already a CI check; the CI gate is the concrete enforcement for the automatable portion. |
| gate2-blocks-own-redaction-fixtures.md | check | Unit test: scanStaged called with a staged set that includes src/*.test.ts fixtures must not flag them; assert only files under resolveDocsRoot are scanned | mage-memory src/staged-scan.test.ts (CI) | | The scope bug is already fixed; the concrete enforcement is the test that proves scanStaged scoping holds for the fixture case. |
| gate2-fp-blocks-autonomy.md | rule | AGENTS.md rule: "never disable or remove the Gate-2 pre-commit hook to unblock yourself; report the exact blocked command and surface it for the human to run" | mage-memory AGENTS.md | | An autonomous agent's decision to remove a hook is purely behavioral; no hook can prevent itself from being removed by the agent managing it. |
| gemini-reindents-whole-files-with-tabs.md | check | Post-delegation verification: grep -c "^$(printf '\t')" <touched-files> -- any nonzero count on a 2-space repo fails; add to agy-delegate verification checklist | global CLAUDE.md or agy-delegate skill | | Calibration example: a diff-stat/tab-count check after every agy run catches the reindent before the commit. |
| harness-memory-layer-rewrites-mage-frontmatter.md | hook | PostToolUse hook on Write/Edit tools: if the written file is under mage/ and contains "name: \"\"" or "node_type: memory" in its frontmatter, run `mage flatten` to normalize | harness settings PostToolUse | | The damage happens the moment the Write/Edit tool completes; a PostToolUse hook is the earliest point at which the rewrite can be detected and repaired. |
| harness-notifications-pollute-the-corrections-lens.md | check | Unit test in distill: assert that corrections-lens output excludes signals starting with <task-notification> or containing <summary>Monitor event:; include a fixture with both types | mage-memory src/distill test (CI) | | The durable fix is at capture (ADR-0015 territory); until it ships a test that proves the filter exists is the concrete enforcement. |
| link-harvests-must-genre-filter-sources.md | check | Unit test: any link-harvest function has a test asserting N fully cross-linked ADR-genre notes + k memory-genre sources yields exactly k harvested results | mage-memory src (CI) | | The failure was a code bug in the harvest filter; the regression test pinning it is the concrete check. |
| mage-integration-test-framework.md | note | (no mechanism) | n/a | When adding a new CLI command or external-tool integration and choosing where to put its test and how to avoid the stdin hang | The note documents the test layout and the stdin-close gotcha; the trigger is exactly when an agent writes a new integration or live test. |
| mage-is-durable-memory.md | delete | Charter/principle document; its lessons are expressed in ADRs 0004, 0005, and 0035 which are ratified and implemented. | n/a | | A charter document defines terms; the specific behavioral lessons all point back to authoritative ADRs. No single enforcement moment exists. |
| mage-main-branch-protected.md | impossible | GitHub branch protection ruleset id 17441632 refuses direct pushes to main; no bypass actors; force-push and deletion blocked. | github.com/Sumit1993/mage-memory branch protection | | Calibration example: GitHub branch protection already refuses the push; nothing for an agent to remember. |
| mage-no-biome-2space.md | hook | PreToolUse deny rule on bash commands matching biome or prettier targeting files in mage-memory | harness settings PreToolUse deny | | A deny rule on the bash tool prevents running biome/prettier at the moment the command is proposed, before any reformatting damage occurs. |
| mature-kb-emits-no-capture-terminals.md | note | (no mechanism) | n/a | When the a1-bake gate shows zero denominator for keep-rate and the operator considers it a soak failure | The note describes a structural property of a mature KB that makes the gate report uncalibratable; the trigger is precisely "keep-rate gate denominator is zero." |
| migration-field-notes.md | note | (no mechanism) | n/a | When migrating external notes into mage for the first time and deciding whether verbatim copy violates ADR-0004 | The recipe and ADR-0004 clarification are only needed at migration time; the trigger is "agent is about to migrate a body of external notes." |
| no-emojis-in-releases.md | hook | PreToolUse hook on bash commands containing "gh release create" or "gh release edit": scan the --notes or --notes-file argument for emoji codepoints and block if found | harness settings PreToolUse deny | | The failure occurs the moment the release command is run; a PreToolUse hook can reject the content before it is published. |
| npx-mage-runs-the-published-release.md | note | (no mechanism) | n/a | When verifying local working-tree changes to mage and deciding between npx mage, bare mage, or node dist/cli.js | The binary-resolution trap only matters at "I am about to verify my working-tree changes" -- exactly the question the agent asks before each verification step. |
| plan-v0.1-locks.md | delete | Point-in-time planning document; all naming and lock decisions are implemented; superseded by the running codebase and ADR-0008. | n/a | | All decisions in this file are resolved: package name, brand, MAP.md dropped, constitution promoted. Nothing actionable remains. |
| plugin-directory-source-copies-untracked-tree.md | rule | AGENTS.md rule: "register the mage marketplace by GitHub source (Sumit1993/mage-memory), never by local directory path; drop the cache before crediting skill behaviour to current work" | global CLAUDE.md or mage-memory AGENTS.md | | No hook can intercept the /plugin marketplace add ./path UI command; the rule must be stated so the agent picks the right form at plugin-registration time. |
| prefer-the-repos-lock-free-convention.md | rule | AGENTS.md rule: "on any hook-invoked path, append one JSONL line and fold on read; never read-modify-write or compact in place; rotate (rename to archive) instead" | mage-memory AGENTS.md | | The concurrency pattern choice cannot be detected by a linter; the rule guides design review when adding a new writer to a shared hook-invoked path. |
| promote-folds-mechanical-tokens.md | note | (no mechanism) | n/a | When a mage:groom run returns a large bucket count with zero proposals and an operator considers grinding the backlog | The diagnosis (high volume + zero value = mechanism problem, not a backlog) only matters at that specific groom output; ADR-0038 owns the remedy. |
| redaction-anthropic-key-detector.md | check | src/redact.test.ts POSITIVES and RAW_SECRETS arrays must include a fixture with hyphens and underscores in the key body; the never-leak loop is the regression guard | mage-memory src/redact.test.ts (CI) | | The fix is in code; the regression test with a hyphen/underscore-containing body is the concrete check that must stay. |
| release-bump-touches-many-artifacts.md | delete | The note itself says release-please now owns the bump and src/release-consistency.test.ts backstops it; the lesson is already enforced. | n/a | | Calibration example: the note explicitly states release-please automates the bump and CI tests the consistency. |
| replay-gate-methodology.md | note | (no mechanism) | n/a | When designing or enabling a new capture heuristic or detector and deciding whether to ship it or run a replay gate first | The replay-gate procedure is invoked exactly "before shipping or default-enabling any new detector/digest/capture heuristic" -- a precise trigger. |
| route-memories-to-the-matching-store.md | rule | AGENTS.md rule: "before writing a note ask: future agents in this repo only, or in any repo? route repo-scoped lessons to mage/notes/, user-scoped lessons to global CLAUDE.md or claude-kit skill" | global CLAUDE.md | | Routing scope is a judgment call at note-write time; no program can determine whether a lesson is repo-scoped or user-scoped. |
| soak-monitor-blind-spots.md | check | soak-report.mjs: assert live-unit count matches soak-targets.md roster; falsify "not yet built" footers with find before printing; exclude baseline:true entries from keep-rate | ~/ai-context/mage-soak/soak-report.mjs | | Three concrete verifiable checks are named; all are automatable in the monitor script. |
| soak-targets.md | note | (no mechanism) | n/a | When running or interpreting the soak monitor rollup and needing to know which units should be live | The roster of soak targets must be consulted whenever a rollup is read; the trigger is "I am reading a soak rollup." |
| status-vocabulary-drift-undercounts-filters.md | check | Unit test: harvest filter test suite must include a fixture with status: active (legacy value) and assert it is included in results; pinned by the extreme review finding | mage-memory src (CI) | | The bug is a code filter that missed a vocabulary variant; a test with the legacy value pinned to the filter is the concrete regression check. |
| stopped-background-workflows-leave-no-record.md | rule | AGENTS.md rule: "any work expected to outlive a turn must write durable evidence (log + exit sentinel, artifact, or commit); never diagnose from absence -- check the artifact the workflow was supposed to produce" | global CLAUDE.md or mage-memory AGENTS.md | | No hook can intercept a UI-level TaskStop; the anti-stall doctrine is a design rule for how background work is structured. |
| test-files-were-excluded-from-typecheck.md | check | tsconfig.json must not list **/*.test.ts in exclude; CI pnpm typecheck then gates test-file type errors automatically | mage-memory tsconfig.json + .github/workflows/ci.yml | | The fix is already applied; the check is the existing CI typecheck job that now covers test files. |
| unreachable-constant-reports-a-false-state.md | check | Unit test: for every default-parameter path, add a test variant that omits the parameter and asserts the default value is used; tests that always supply the value cannot catch wiring bugs | mage-memory src unit tests (CI) | | The lesson reduces to "add the test that omits the parameter"; that specific test shape is the concrete check. |
| weakening-a-test-can-delete-its-purpose.md | rule | AGENTS.md rule: "when a behaviour change breaks a test, ask: would this test still fail if the feature it names were deleted? if no, rebuild the fixture or delete the test honestly" | mage-memory AGENTS.md | | The judgment of whether an assertion is vacuous requires understanding intent; no linter can distinguish a tautology from a real assertion. |

## Uncertain

- **connect-doesnt-ensure-ignores.md**: check vs hook. The note describes a code fix (ensureGitignored called inside connect), which after the fix makes the failure impossible. Routed to check because the test proving the fix holds is the durable enforcement regardless of whether the fix is shipped.
- **mage-no-biome-2space.md**: hook vs check. A diff-stat check after every agy run (see gemini-reindents-whole-files-with-tabs.md) could also catch this. Routed to hook because a PreToolUse deny prevents damage rather than detecting it after the fact.
- **harness-memory-layer-rewrites-mage-frontmatter.md**: hook vs impossible. A deny rule on Write/Edit under mage/ when autoMemoryDirectory is set would make it impossible. Routed to hook because a total deny would also block legitimate CLI-mediated edits; a rewrite hook is safer.
- **no-emojis-in-releases.md**: hook vs rule. The preference is about a command a hook can intercept. Routed to hook because the failure is detectable at the exact moment the release command runs; a rule is the fallback if the hook proves fragile.
- **dogfood-before-release.md**: check vs rule. The CI gate (build + test + typecheck) is real and enforced. The smoke-test against real inputs cannot be automated. Routed to check because the CI portion is the concrete enforcement; the manual smoke step is a documented procedure, not a note-level memory.
