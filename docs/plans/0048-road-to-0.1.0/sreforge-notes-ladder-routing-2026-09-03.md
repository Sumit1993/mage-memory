| rung | count |
|---|---|
| impossible | 4 |
| check | 19 |
| hook | 0 |
| rule | 18 |
| note | 14 |
| delete | 3 |

| file | rung | mechanism | where it lands | trigger moment | why |
|---|---|---|---|---|---|
| grep-completeness-bypass-gitignore.md | rule | Ops rule: for completeness/verification sweeps use "command grep -rIn <pattern> <dir> --exclude-dir=.git --exclude-dir=node_modules" (bypasses the gitignore-aware wrapper) and never an --include filter | user kit (claude-kit) |  | Shell-environment quirk (a local grep wrapper) an agent must remember to route around; not sreforge-specific |
| prune-docs-dont-append.md | rule | Docs-hygiene rule: when a decision/plan changes, delete the obsolete content and restate current truth instead of appending supersession annotations; git is the archive | user kit (claude-kit) |  | The file's own banner says this is host-global, not sreforge content, and is pending promotion to ~/.claude/CLAUDE.md |
| reuse-tools-research-first.md | rule | Standing rule: prefer battle-tested tools over hand-rolling; research current options via the web before recommending, never suggest from memory | user kit (claude-kit) |  | The file's own banner says this applies overall and is pending promotion to ~/.claude/CLAUDE.md |
| what-next-questions-are-questions-not-change-requests.md | rule | Conversational-protocol rule: a status/direction question ("what next", "what's pending") wants an answer, not changes; act only on an explicit go | user kit (claude-kit) |  | The file's own banner flags this as zero-SREForge-content and pending promotion to global CLAUDE.md; duplicates hub1's ask-vs-act-on-status-questions.md |
| ablation-ladder-is-the-discrimination-gate.md | check | Add a gate in the eval scenario-authoring pipeline requiring an L1 measurement (tool loop only, no skill or supervisor) at n>=10 showing pass rate <=30% or documented spread before a scenario is admitted to the harness A/B set | sreforge code repo |  | Concrete quantitative admission gate already stated as the rule |
| agent-cmd-is-a-taskfile-var-not-an-env-var.md | impossible | Make the Taskfile task read AGENT_CMD from the environment as a fallback default so exporting it in the shell actually takes effect instead of being silently ignored | sreforge code repo |  | A Taskfile default-from-env fix removes the misattribution class structurally |
| agent-driver-tiers.md | note | Read when choosing which driver to point at a new eval scenario | hub itself | When picking or debugging which agent driver (agent-loop, agy, PrismaLens) runs a given scenario | Reference map of driver roles and the gotchas around each, not a single correctable lesson |
| agent-environment-boundary.md | delete | n/a | n/a |  | The hub's own genre-audit comment retypes this from pointer to doc and states explicitly it is "not a recallable memory" - the one recallable gotcha was already split out to sysbox-no-go-under-rancher-desktop.md |
| agent-integration-contract.md | note | Read when building a new use-case's agent integration seam (transports, telemetry, egress) | hub itself | When designing how a new use-case exposes itself to an agent | Ratified design contract (ADR-0018), canonical reference for the integration boundary |
| agy-gemini-quota-cycles.md | rule | Ops rule: smoke-test agy Gemini before any batch; launch big batches right after a quota reset, not before one; on quota death mid-job check the worktree/artifacts first before re-running | user kit (claude-kit) |  | agy usage discipline applies to any repo, not sreforge-specific |
| agy-silent-death-work-intact.md | rule | Fold as a third documented failure mode into the agy-delegate skill: a 0-byte stdout log with no commit can still mean the work landed; git status the worktree first, assume zero verification happened, and review it yourself | user kit (claude-kit) |  | Distinct from the two failure modes the skill already documents; generalizes past sreforge |
| alert-constants-are-cross-scenario-contracts.md | check | Add a validation step in scenario/alert-rule authoring that checks a new or changed alert threshold against every scenario's healthy-traffic baseline, not only the authoring scenario's | sreforge code repo |  | The fix shape (self-relative collapse + grader scoping) already landed for the one known instance; generalize the check |
| all-seats-route-to-agy.md | delete | n/a | n/a |  | Per the note network's own record (see agy-gemini-quota-cycles.md relations), this rule was re-homed to the global CLAUDE.md "Execution loop" section on 2026-07-27 |
| ambient-furniture-can-fire-the-trigger-webhook.md | check | Add a role: ambient filter to the trigger-selection component, matching what confirm-quiesced already does; tracked as sreforge#107 | sreforge code repo |  | Concrete, already-identified code fix using a label that already exists for exactly this purpose |
| architecture.md | note | Read when extending the SREForge engine, adding a use-case, or understanding the incident/patch run lifecycle | hub itself | When designing a new engine component or use-case | Canonical architecture reference, not a single correctable lesson |
| assert-the-character-class-not-the-known-leaks.md | rule | Guard-writing rule: for a public artifact derived from private data, assert the absence of a whole character class (e.g. zero digits) rather than a blocklist of known-bad values; test the guard by adding a leak | sreforge code repo |  | Generalizable test-design heuristic no program can pick for you |
| astro-docs-on-github-pages-base-path-leaks-404s.md | check | Add a post-build CI check that crawls the built docs site's internal links for 404s under the base path, and asserts docs/dist/CNAME exists before deploy | sreforge code repo |  | Both traps (missing base prefix, dropped CNAME) are mechanically detectable in the build output |
| benchmark-substrate-precedent.md | note | Read when selecting a new substrate app or defending substrate-selection methodology against a field comparison | hub itself | When choosing or justifying a substrate/benchmark app choice | Survey conclusion meant to prevent re-surveying, not a single correctable lesson |
| build-invariants.md | check | Already enforced programmatically for invariants 1 and 4 by tools/contamination-guard (regex tripwire) and tools/detell-judge (3-axis LLM-judge gate); invariants 2/3/5/6 are ADR-linked review gates before a new substrate or scenario merges | sreforge code repo |  | The note documents its own enforcement mechanism explicitly |
| build-specs-must-name-docs-surfaces.md | check | Already backed by CI/template checks: a .coderabbit.yaml docs-drift path rule, a required Docs-impact field on the issue template, and a PR-template checklist item; the exact rule was also promoted into mage-memory's own AGENTS.md Docs-surfaces requirement | sreforge code repo |  | Multiple concrete backstops already exist; the note itself says the spec is still the primary fix |
| campaign-cold-session-default.md | rule | Already the batch.sh pattern: launch one fresh driver process per graded campaign run, never loop runs inside one agent session; pair with the quiesce gate between arms | sreforge code repo |  | Concrete operational rule already followed by the batch scripts |
| ci-timeout-dead-runner-signature.md | check | Add a pre-verdict check: before banking a "rejected 0.000, ci was not green" verdict after a 10-minute CI timeout, verify the act-runner container is actually up (docker ps) and auto-recover before re-grading | sreforge code repo |  | A dead-runner signature is mechanically distinguishable from a genuinely bad fix |
| contamination-control-prior-art.md | note | Read when questioning whether the de-tell/contamination approach is missing a layer other benchmark harnesses use | hub itself | When redesigning contamination controls or defending the current 3-layer model | Literature survey done so the field is not re-surveyed |
| contamination-guard-blind-surfaces.md | check | Extend tools/contamination-guard/scan.mjs with structural git probes (branch names, commit-timestamp clustering, lockfile coherence) and optional live-runtime probes, since a PASS today only scans substrate TEXT | sreforge code repo |  | Each named blind spot is a mechanically addable probe |
| de-tell-hunt-procedure.md | rule | Already the codified de-tell hunt workflow: for every new harness-side artifact, run the parallel-finders-per-surface, adversarial-verify, completeness-critic loop against the 8-surface catalog until dry, then verify on the live deploy plane | sreforge code repo |  | Operationalized multi-agent workflow; no program decides when to invoke it |
| de-tell-lens-dominates.md | rule | Design-review rule: before endorsing any substrate, infra, or topology choice, ask what the agent would see and whether it reveals the rig; de-tell outranks convenience arguments | sreforge code repo |  | Judgment call on every architecture decision, not mechanically checkable |
| decoy-anchor-trap.md | check | Structural fix (issue #96 PR): give decoy-deploy-control its own prepare-decoy-base step and seed the decoy in mode-3 arming, so the scenario cannot silently run with dead physics | sreforge code repo |  | Concrete, already-scoped code fix for a named discriminator scenario |
| dst-applicability.md | note | Read when considering deterministic simulation testing for the harness engine or agent environment | hub itself | When evaluating whether DST fits a new part of the harness | Comparative verdict; the harness-side ideas that do transplant are already tracked under ADR-0010 |
| eval-contract.md | note | Read when writing or debugging a new scenario's setup-to-engine handoff | hub itself | When authoring a new scenario or changing the run contract shape | Canonical schema/contract reference between scenario setup and the engine |
| extension-recipes.md | rule | Already surfaced as a pointer in the generated mage-wing-sreforge skill: the copy-pasteable step lists for adding a substrate or a scenario are the skill's canonical recipe | sreforge code repo |  | Already promoted to skill-tier per the hub's own grooming note |
| forge-credential-drift.md | check | Add a "forge doctor" identity/health check (adjacent to issue #52) that detects credential drift between gitea and the stack .env and reconciles it automatically instead of requiring the manual password-reconcile recipe on every recreate | sreforge code repo |  | A concrete diagnostic-and-fix script is nameable from the symptoms already documented |
| glossary.md | note | Read when writing or reading any SREForge doc to resolve an overloaded term | hub itself | When a term in SREForge documentation is ambiguous or overloaded | Canonical terminology reference, not a single correctable lesson |
| go-task-not-on-path-in-worktrees.md | rule | Already decided (decisions/0017): use the neutral dispatcher pnpm forge <verb> <use-case>, which never invokes bare task directly and cannot hit this trap; falls back to pnpm exec task or the direct binary path when the dispatcher does not apply | sreforge code repo |  | A per-worktree PATH gap that the existing dispatcher decision already routes around |
| harness-identity-gap.md | check | Fix tracked as sreforge#176: record a hashable harness manifest (harness_manifest_sha256, kickoff shape, skill_ref) in every run's handoff header so two runs sharing a harness/model string can be verified comparable | sreforge code repo |  | Concrete, already-scoped record-schema fix |
| measurement-store-split.md | check | Fix tracked as sreforge#177: unify the A/B measurement into one store so (scenario, harness, model) can be censused without a script; until then check which store a delta came from and whether its header carries harness, model, and confinement before quoting it | sreforge code repo |  | Concrete, already-scoped fix; the interim discipline is a checkable precondition on any quoted delta |
| node-fetch-ignores-proxy-env-false-negative.md | check | Add a lint/convention check flagging Node fetch-based egress or proxy verification probes in test/verify scripts; require curl or an explicitly proxy-aware client for any claim that a boundary held | sreforge code repo |  | Grep-detectable pattern (fetch used where a proxy-aware probe is required) |
| owner-frames-sreforge-as-harness-bench.md | rule | Methodology rule: every eval proposal names what varies (model, harness, or both) and refuses to compare runs that vary both; judge scenario work by whether it separates harnesses, not by raw pass rate | sreforge code repo |  | Framing/judgment discipline for eval design, not mechanically checkable |
| port-hijack-masquerades-as-forge-failure.md | impossible | Already named: set remote.portsAttributes for port 3000 to onAutoForward: ignore in ~/.vscode-server/data/Machine/settings.json, so VS Code can never again win the startup race and hijack the port | sreforge code repo |  | A machine-config change removes the startup-race class entirely rather than relying on the one-second diagnostic every time |
| pr-review-response-protocol.md | rule | AGENTS.md rule: for each inline PR review comment, reply describing the fix before resolving, never resolve silently; post under the agent's own GitHub identity; announce each outward post in chat | user kit (claude-kit) |  | Overlaps with and predates the now-canonical claude-kit pr-review-process.html living doc |
| prismalens-eval-ground.md | note | Read when prioritizing which scenario to build next for the PrismaLens eval | hub itself | When sequencing SREForge scenario work against PrismaLens's eval needs | Cross-hub pointer recording a cross-repo decision, not a standalone lesson |
| rancher-wsl-daemon-cycle-signature.md | check | Add a health-check/recovery script that detects "all deploy containers exited 255 simultaneously with no OOM" as the Rancher/WSL daemon-cycle signature and runs the wait-for-socket plus forge-up plus deploy-up recovery automatically | sreforge code repo |  | The recovery sequence is already fully specified and scriptable |
| rca-judge-rescore-without-rerun.md | rule | Already the documented procedure: node tools/headroom/campaign.mjs score --judge (or rca-judge/judge.mjs directly) re-issues a banked run's diagnosis score without re-running; RCA_JUDGE_MODEL must be set explicitly to a non-Claude model per ADR-0027 | sreforge code repo |  | Fully specified CLI procedure already built; nothing forces an agent to know it exists rather than re-running the scenario |
| record-store.md | check | Add a test asserting bank.mjs's hardcoded org and repo allowlist (the --store regex and the privacy-assertion path) matches the actual configured remote, so an org rename is caught by CI instead of failing closed at runtime with a misleading message | sreforge code repo |  | Store contract reference overall, but the org-rename gotcha at the end has a concrete, nameable fix |
| runner-stale-shim-mount-recovery.md | check | Fix tracked as sreforge#53: have the forge-plane verb detect the stale-shim-mount OCI error and rm -f the container itself before retrying up -d, instead of requiring a human to remember the two-step recovery | sreforge code repo |  | Already-scoped automation fix for a fully diagnosed failure signature |
| sandbox-runtime-tanstack-evaluation.md | note | Read when configuring srt for host-side driver confinement or evaluating a third-party sandbox/agent-runner package | hub itself | When wrapping a driver in srt, or when a new sandbox package is proposed | Technical evaluation with many config gotchas and a still-open "record confinement as enforced/cooperative" action item, but no single correctable lesson dominates |
| scenario-fault-mechanism-gotchas.md | rule | Scenario-design rule: verify a fault's physical lever against the real substrate before locking a scenario design (worker concurrency model, READINESS_GATE=on for relentless faults, monotonic backdated-commit dates) | sreforge code repo |  | Substrate-physics judgment calls that need domain reasoning per fault, not a single mechanical check |
| seed-volume-destabilizes-the-healthy-baseline.md | check | Add a pre-arm baseline-quiet check: after seeding, confirm the healthy baseline shows no alert firing and that post-arm firing time is not suspiciously short, before accepting a scenario's seed volume | sreforge code repo |  | Concrete, scriptable precondition on scenario tuning |
| single-file-bind-mounts-break-on-rancher-recreate.md | rule | Already the applied convention across the booklogr stack: never bind-mount a single file on WSL2/Rancher, bind the parent directory or bake the file into the image instead | sreforge code repo |  | Convention already retrofitted everywhere it applied; new compose additions must still follow it, and nothing enforces that automatically |
| spec.md | note | Start here when orienting to what SREForge is and how the hub is organized | hub itself | When a new session or contributor needs the entry point into the SREForge docs | Entry-point overview linking to the rest of the canonical notes |
| sregym-reference.md | note | Read when deciding whether to adopt a SREGym pattern (oracle design, agents.yaml registry, scenario shape) | hub itself | When borrowing or explicitly rejecting a pattern from SREGym | Prior-art reference with a ranked borrow list and a do-not-copy list |
| stack-gotchas.md | delete | n/a | n/a |  | Pure index to nine notes already split out and separately routed (ADR-0041 curation); nothing left to route here |
| stale-env-reroutes-provider.md | check | Add a pre-run assertion: before any run measuring cross-service traffic, grep the stack .env for DATA_PROVIDER overrides and confirm Prometheus shows nonzero book_metadata_requests_total | sreforge code repo |  | Concrete, scriptable precondition already given as the manual check to run |
| sysbox-no-go-under-rancher-desktop.md | note | Read when someone proposes or attempts a Sysbox migration on this Rancher Desktop box | hub itself | When Sysbox setup is proposed or a Sysbox registration attempt fails mysteriously | Verified dead end explicitly marked "do not re-investigate"; the current by-omission boundary is already built and live-verified |
| topology-extension.md | note | Read when considering extending SREForge to a k8s or VM deploy topology | hub itself | When a new topology (k8s, VM) is proposed for the harness | Feasibility assessment of the CdDeployer/Cleanup seam, not a single correctable lesson |
| verify-the-deployed-artifact-not-the-source.md | check | Verify de-tell/mtime facts IN THE IMAGE, not the working tree, with a whole-second threshold excluding __pycache__; rebuild --no-cache once to clear a poisoned BuildKit layer, and never call a fix done until confirmed on the live deploy plane | sreforge code repo |  | Concrete, scriptable verification discipline |
| wsl-binfmt-interop-lost-reregister.md | impossible | Add a systemd/WSL startup hook that re-registers the WSLInterop binfmt entry automatically on distro start, instead of requiring a human to notice Rancher won't launch and run the one-liner | sreforge code repo |  | A startup hook removes the manual-recovery step entirely |
| wsl-box-shell-gotchas.md | rule | Shell convention on this WSL box: never use find -newermt (bfs find lacks relative-time support, use -newer <marker-file> instead); a kill/pkill slug must not appear anywhere else in the same command line even bracket-escaped in only one spot | user kit (claude-kit) |  | General shell-portability gotchas applicable to any WSL work, not sreforge-specific |
| wsl-interop-docker-breakage.md | impossible | Commit the docker-credential-wincred.exe shim script into the sreforge repo's dev-setup and have the environment-setup script install it on PATH automatically, so WSL interop breakage no longer blocks docker pulls without manual intervention | sreforge code repo |  | A committed, auto-installed shim makes the workaround structural instead of something to remember |

## Uncertain

- agent-environment-boundary.md - delete vs note. Routed delete because the hub's own genre-audit comment explicitly disclaims it as "not a recallable memory" and says the one recallable gotcha was split to sysbox-no-go-under-rancher-desktop.md; a case remains for note since the hub curators chose to retype and keep the file rather than delete it outright.
- all-seats-route-to-agy.md - delete vs rule. Routed delete on the strength of a note-network cross-reference (in agy-gemini-quota-cycles.md) saying this rule was re-homed to global CLAUDE.md and the note deleted; the file still physically exists in the hub, so the re-home may not be complete.
- prune-docs-dont-append.md, reuse-tools-research-first.md, what-next-questions-are-questions-not-change-requests.md - rule vs delete. Each carries its own "re-home pending, promote then delete" banner; routed rule (landing at user kit) rather than delete because the promotion has not actually happened yet per the banners' own admission.
- sandbox-runtime-tanstack-evaluation.md - note vs check. The "record confinement as enforced/cooperative in the run record" item is a concrete, still-unbuilt, named action; a case exists for check, but the note's dominant character is a broad technical evaluation rather than one lesson.
- sysbox-no-go-under-rancher-desktop.md - note vs rule. Routed note on the trigger-moment test (prevents re-investigating a dead end), but the "what to do instead" guidance is already fully built and live-verified practice, which also fits rule.
