name: Dependabot auto-merge

on: pull_request

# This is a managed caller stub.
# The lane logic lives in prismalens/gh-workflows/.github/workflows/dependabot-auto-merge.yml.
# Do not add logic here.

jobs:
  auto-merge:
    uses: prismalens/gh-workflows/.github/workflows/dependabot-auto-merge.yml@main
    permissions:
      contents: write
      pull-requests: write
